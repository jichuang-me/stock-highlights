# Stock Highlights environment template
# Copy this file to `.env` for local development, or set the same keys in Hugging Face Space Secrets.

# Built-in server models use the Hugging Face Router only.
# Paid or self-hosted models should be added by users in the app UI.
HF_TOKEN=your_huggingface_token

# Optional runtime tuning
REQUEST_TIMEOUT=8
CNINFO_DAYS_DEFAULT=180
CORS_ALLOW_ORIGINS=*
